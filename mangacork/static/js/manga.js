var IMAGE_DIR = '/static/images';
var CHAPTER = '/dragonball_ch1';

// Set current page w/ cookies
// Therefore if user comes back then the
// page will remember where they left off
// If no cookie then serve first page

function changePage(){
	alert("Clicked");
}